// 3.1.1

const num = 18763
const numStr = num.toString()



for (let i = 0; i < numStr.length - 1; i++) {
    const element = numStr[i];
      
    if (element < numStr[i+1] ) {
       console.log(numStr);
 
    
    }
}


